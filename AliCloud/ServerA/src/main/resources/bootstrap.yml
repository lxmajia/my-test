server:
  port: 8080
spring:
  application:
    name: server-a
  cloud:
    nacos:
      discovery:
        server-addr: server.xwlin.cn:8848
        cluster-name: DEFAULT
        namespace: product
      config:
        server-addr: server.xwlin.cn:8848
        cluster-name: DEFAULT
        namespace: product
